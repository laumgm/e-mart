paginate

<LinkContainer 
  key={x + 1} 
  to={(!isAdmin && !isSeller) ? 
    keyword ? `/search/${keyword}/page/${x+1}` 
    : category ? `/category/${category}/page/${x+1}`
    : `/page/${x+1}` 
    : isAdmin ?`/admin/productlist/${x+1}`
    : `/produclist/seller/${x+1}`
  }
>